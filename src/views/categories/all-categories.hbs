{{#each categories}}
<article class="row no-gutters bg-light position-relative shadow m-3">
    <div class="col-md-6 mb-md-0 p-md-4">
        <img src="{{images}}" class="w-75 d-flex mx-auto" alt="{{name}}">
    </div>
    <div class="col-md-6 position-static p-4 pl-md-0 ">
        <h3 class="m-2 text-center">{{name}}
        </h3>

        <p class="spacing-text">{{description}}</p>
        <form action="/categories/c/{{_id}}" method="GET" class="mx-auto">
            <button type="submit" class="btn btn-secondary btn-md ">Ver más</button>
        </form>
        <div class="d-flex justify-content-end">

            <form action="/categories/delete/{{_id}}?_method=DELETE" method="POST" class="mx-2">
                <input type="hidden" name="__method" value="DELETE">
                <button type="submit" class="btn btn-danger btn-md ">Borrar</button>
            </form>

            <a class="d-flex" href="/categories/edit/{{_id}}"><i
                    class="fas fa-pencil-alt text-primary mx-2"></i></a>
        </div>

    </div>
</article>
{{/each}}

{{!-- <div class="row justify-content-center">
    {{#each categories}}
    <div class="col-lg-3 py-3 d-flex align-items-stretch ">
        <div class="card shadow ">
            <img src="{{images}}" class="card-img-top" alt="...">
            <div class="card-body">
                <h4 class="card-title text-center">
                    {{name}}
                    <a href="/categories/edit/{{_id}}"><i class="fas fa-pencil-alt text-primary"></i></a>
                </h4>
                <form action="/categories/delete/{{_id}}?_method=DELETE" method="POST"
                    class="d-flex justify-content-center">
                    <input type="hidden" name="__method" value="DELETE">
                    <button type="submit" class="btn btn-danger btn-md">Borrar</button>
                </form>
                <form action="/categories/{{_id}}" method="GET"
                    class="d-flex justify-content-center align-content-center">
                    <button type="submit" class="btn btn-info btn-md">Saber más</button>
                </form>
            </div>
        </div>
    </div>
    {{/each}}
</div> --}}
{{!-- 


{{!-- {{#each categories}}
<div class="row no-gutters bg-light position-relative shadow m-3">
    <div class="card bg-dark text-white">
        <img src="{{images}}" class="card-img w-50" alt="{{name}}">
        <div class="card-img-overlay">
            <h5 class="card-title">{{name}}</h5>
            <p class="card-text">{{description}}</p>
        </div>
    </div>
</div>
{{/each}} --}}